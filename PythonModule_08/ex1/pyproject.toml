[project]
name = "loading"
dynamic = [ "version" ]
requires-python = ">=3.10"
dependencies = [
    "matplotlib<=3.10.8",
    "pandas<=2.3.3",
    "requests<=2.32.5",
	"numpy<=1.25.0"
]

[tool.poetry]
version = "1.0"